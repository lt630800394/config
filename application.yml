spring:
  application:
    name: config-server
  cloud:
   config:
    server:
     git:
       uri: https://github.com/forezp/SpringcloudConfig/
       search-paths: respo
#       username: username
#       password: password
    label: master
server:
  port: 8010
